<ul>
  <% @tasks.each do |t| %>
    <li><%= t.title %></li>
    <% end %>
</ul>

<%= form_for UserTask.new do |f| %>
  <%= f.collection_select :task_id, Task.all, :id, :title %>
  <%= f.submit %>
<% end %>
